# ── Session planning docs (not part of the public repo) ──
/docs

# ── OS / editor cruft ──
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# ── Rust build artifacts ──
backend/target/

# ── Embedding model cache (downloaded by fastembed at runtime) ──
.fastembed_cache/

# ── Generated binary data (regenerated from db/movie_metadata.csv) ──
db/embeddings.bin

# ── Frontend build artifacts & deps ──
frontend/node_modules/
frontend/dist/
*.tsbuildinfo
